# Visual References

- 이 디렉토리는 버전 관리가 필요한 디자인/기획 레퍼런스 이미지를 보관한다.
- 문서에서 직접 참조하는 결과 스크린샷은 `docs/screenshots/`를 canonical 경로로 사용한다.
- 로컬 임시 캡처나 검토용 산출물은 `tmp/` 또는 ignore 대상 경로에 두고 여기로 올리지 않는다.
- `handoff-sm-new-direction/`은 `handoff-2026-04-25/`에서 fork한 candidate reference pack이다. 현재 정리된 진입점은 `handoff-sm-new-direction/HANDOFF.md`다.
- `sm-new-api-db-baseline.md`는 `Team Design > 1차 디자인 완료` 기준으로 API/DB 설계를 시작하기 위한 기준 선언 문서다. 기존 schema/API 문서는 구현 증거로만 보고, 신규 설계 기준으로 승격하지 않는다.
